VITE_COLYSEUS_URL=ws://localhost:2567

# --- Variabili SOLO per la Vercel Function (client/api/auth/telegram.ts) ---
# Queste NON hanno prefisso VITE_: non finiscono nel bundle del browser.
# Da impostare su Vercel → Project Settings → Environment Variables.
# Qui servono solo se usi `vercel dev` in locale.
TELEGRAM_BOT_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
